'use client';

import { RenaultCommissioningForm } from '@/components/form/RenaultCommissioningForm';

export default function Page() {
    return (
        <main>
            <RenaultCommissioningForm />
        </main>
    );
}